# {项目名} — 技术架构方案

## 1. 系统架构

### 架构总览
{整体系统拓扑描述，模块间关系}

### 架构图
```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   前端应用    │────▶│   API 网关    │────▶│   后端服务    │
└──────────────┘     └──────────────┘     └──────────────┘
                                                │
                              ┌─────────────────┼─────────────────┐
                              ▼                 ▼                 ▼
                     ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
                     │   数据库      │  │    缓存       │  │  消息队列     │
                     └──────────────┘  └──────────────┘  └──────────────┘
```

## 2. 技术选型

### {模块1} 技术选型

| 方案 | 功能匹配 | 性能 | 社区 | 学习成本 | 运维 | 总分 |
|------|---------|------|------|---------|------|------|
| 方案A | | | | | | |
| 方案B | | | | | | |

**选择**: {方案X}
**理由**: {为什么选这个}
**代价**: {放弃了什么}
**缓解**: {如何缓解不足}

### {模块2} 技术选型
{同上格式}

### 技术栈总览
| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端 | | | |
| 后端 | | | |
| 数据库 | | | |
| 缓存 | | | |
| 消息队列 | | | |
| 部署 | | | |

## 3. 数据模型

### 实体关系图
{ER 图描述}

### 核心表设计

#### {表名1}
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| uuid | VARCHAR(36) | UNIQUE, NOT NULL | 对外暴露ID |
| {字段} | {类型} | {约束} | {说明} |
| created_at | DATETIME | NOT NULL, DEFAULT NOW() | 创建时间 |
| updated_at | DATETIME | NOT NULL, DEFAULT NOW() | 更新时间 |
| deleted_at | DATETIME | NULL | 软删除标记 |

**索引**:
- PRIMARY: id
- UNIQUE: uuid
- INDEX: {查询常用字段}

#### {表名2}
{同上格式}

## 4. API 设计

### 统一响应格式
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "meta": { "page": 1, "size": 20, "total": 100 }
}
```

### 错误码体系
| 错误码 | HTTP 状态 | 说明 |
|--------|----------|------|
| SYS_001 | 400 | 参数校验失败 |
| AUTH_001 | 401 | Token 过期 |
| PERM_001 | 403 | 无权限 |
| {MODULE}_001 | 404 | {资源}不存在 |

### 接口列表

#### {接口1}
- **方法**: POST /api/v1/{resource}
- **描述**: {功能描述}
- **请求参数**:
  ```json
  {请求体示例}
  ```
- **响应**:
  ```json
  {响应体示例}
  ```
- **错误码**: {可能的错误码}

#### {接口2}
{同上格式}

## 5. 安全措施

### 认证授权
- **方案**: {JWT/OAuth2/Session}
- **Token 格式**: {格式}
- **有效期**: {时长}
- **刷新机制**: {描述}

### 数据安全
- 传输: HTTPS 强制
- 存储: 密码 bcrypt，敏感字段 AES
- 密钥管理: {方案}

### API 安全
- 限流: {策略}
- CORS: {白名单}
- 输入校验: {方案}

## 6. 风险识别

| # | 风险 | 影响 | 概率 | 应对方案 |
|---|------|------|------|---------|
| 1 | {风险1} | 高/中/低 | 高/中/低 | {方案} |
| 2 | {风险2} | | | |
| 3 | {风险3} | | | |

## 7. 代码架构

### 分层设计
```
Controller → Service → Repository → Database
    ↕           ↕           ↕
  DTO        Domain      Entity
```

### 模块划分
| 模块 | 职责 | 依赖 |
|------|------|------|
| {模块1} | {职责} | {依赖} |
| {模块2} | {职责} | {依赖} |

### 目录结构
```
src/
├── controller/     ← API 入口
├── service/        ← 业务逻辑
├── repository/     ← 数据访问
├── model/          ← 实体/DTO
├── config/         ← 配置
├── common/         ← 公共工具
└── exception/      ← 异常定义
```

### 设计模式
| 模式 | 应用场景 | 说明 |
|------|---------|------|
| {模式1} | {场景} | {说明} |

## 8. 并发与边界设计

### 前端防抖/节流
| 场景 | 策略 | 延迟 |
|------|------|------|
| {场景} | 防抖/节流 | {ms} |

### 并发控制
| 场景 | 方案 | 说明 |
|------|------|------|
| {场景} | 乐观锁/分布式锁 | {说明} |

### 边界条件
| 边界类型 | 处理方式 |
|---------|---------|
| 空值输入 | {处理} |
| 空集合 | {处理} |
| 分页边界 | {处理} |

## 9. 架构约束分析

### 技术栈限制
{框架版本约束、兼容性、已知 issue}

### 基础设施约束
{部署环境、资源限制、网络拓扑}

### 团队约束
{技术栈熟悉度、维护成本}

### 时间约束
{MVP 上线时间对架构决策的影响}

## 10. 性能指标设计

| 指标 | 目标值 | 测试方法 |
|------|--------|---------|
| P95 响应时间 | {ms} | {工具} |
| QPS | {数值} | {工具} |
| 数据量级 | {数值} | {预估} |
| 缓存命中率 | {%} | {监控} |

## 11. 安全策略设计

### 认证授权
{OAuth2/JWT/Session 选型、RBAC/ABAC 模型}

### 数据安全
{加密传输、加密存储、密钥管理}

### 审计日志
{操作审计、数据变更审计}

## 12. 扩展性设计

### 水平扩展
{无状态设计、负载均衡}

### 功能扩展
{插件机制、事件驱动}

### 数据扩展
{分库分表方案}

## 13. 技术债务管理

| 债务 | 原因 | 影响 | 偿还计划 |
|------|------|------|---------|
| {债务1} | {为什么妥协} | {影响} | {何时偿还} |

## 14. 可观测性设计

### 日志规范
{结构化日志、级别策略}

### 监控指标
{RED 方法、USE 方法}

### 链路追踪
{Trace ID 透传}

### 告警策略
{告警分级、响应流程}

## 15. 工程健壮性设计

### 超时设计
| 调用类型 | 超时配置 |
|---------|---------|
| HTTP 外部 API | 连接 {X}s，读取 {X}s |
| 数据库 | {X}s |
| Redis | {X}s |

### 缓存策略
| 场景 | 方案 | TTL |
|------|------|-----|
| {场景} | {方案} | {TTL} |

### 限流
| 维度 | 策略 | 阈值 |
|------|------|------|
| 接口级 | {策略} | {QPS} |

### 降级
| 场景 | 降级方案 |
|------|---------|
| {场景} | {方案} |

### 脱敏
| 字段 | 脱敏规则 |
|------|---------|
| {字段} | {规则} |

### 幂等性
| 场景 | 方案 |
|------|------|
| {场景} | {方案} |

### 连接池
| 组件 | 参数 | 值 |
|------|------|-----|
| DB | maximumPoolSize | {值} |

### 文件上传
| 检查项 | 配置 |
|--------|------|
| 类型 | {白名单} |
| 大小 | {上限} |

## 16. 日志/异常/错误处理

### 错误码格式
`{MODULE}_{NUMBER}`

### 异常传播策略
| 层 | 处理 |
|---|------|
| Controller | 不捕获，交给全局处理器 |
| Service | 捕获业务异常，不捕获系统异常 |

### 日志级别
| 级别 | 使用场景 |
|------|---------|
| INFO | 业务关键节点 |
| WARN | 可恢复异常 |
| ERROR | 需要人工介入 |

## 17. 需求映射

| REQ | 架构决策 | 涉及模块 | 备注 |
|-----|---------|---------|------|
| REQ-001 | {决策} | {模块} | {备注} |
| REQ-002 | {决策} | {模块} | {备注} |

## 18. docker-compose.test.yml

```yaml
version: "3.8"
services:
  {按实际服务定义}
```
